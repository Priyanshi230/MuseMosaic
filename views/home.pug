//- we call a pug function by using plus sign(+) and then the name of function with parameter passed.
extends layouts/main-layout.pug  

block content 
 
  +createPostForm(userLoggedIn)
  .postsContainer
  +createReplyModal(userLoggedIn)

block scripts 
    script(src ="/js/home.js")     